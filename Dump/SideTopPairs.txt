<div className="w-1/4 h-[700px] rounded-2xl shadow-2xl border border-gray-800 flex flex-col backdrop-blur-sm bg-black/40 p-3 overflow-y-auto space-y-2">
        {recentAttacks.length === 0 ? (
          <div className="text-center text-gray-400 mt-8 font-mono animate-pulse">
            MONITORING...
          </div>
        ) : (
          recentAttacks.map((attack) => (
            <div
              key={attack.id}
              className="bg-black/50 rounded-lg p-3 border border-gray-700 hover:border-green-400 transition-transform transform hover:scale-105"
            >
              <div className="flex justify-between items-start mb-1">
                <div className="text-xs font-bold text-green-400 truncate font-mono">
                  From: {attack.origin}
                </div>
                <div
                  className={`text-xs px-2 py-0.5 rounded font-mono ${getSeverityColor(
                    attack.severity
                  )} bg-gray-800 border border-current`}
                >
                  {attack.severity.toUpperCase()}
                </div>
              </div>
              <div className="text-xs text-gray-300 mb-1 font-mono">
                To: {attack.target}
              </div>
              <div className="flex justify-between items-end text-xs font-mono">
                <div className="space-y-0.5">
                  <div className="text-gray-400">
                    THREAT:{" "}
                    <span className="text-white">
                      {(attack.value * 100).toFixed(1)}%
                    </span>
                  </div>
                  <div className="text-gray-400">
                    RANK:{" "}
                    <span className="text-yellow-400">#{attack.rank}</span>
                  </div>
                </div>
                <div className="text-gray-500">{attack.time}</div>
              </div>
            </div>
          ))
        )}
      </div>